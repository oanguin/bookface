include ./image

mixin book(book)
    .gtco-section
        .gtco-container
            .row
                .col-lg-11.col-centered
                    div.item
                        if book
                            div.gtco-item
                                h2=book.title
                                +image(book.picture)
                                each author in book.authors
                                    h5=author.last_name + "," + author.first_name

                        else
                            div No Book Found!